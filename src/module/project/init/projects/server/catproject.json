{
    "name": "{{server.name}}",   
        
    "server": {
        "host": "{{server.host}}",
        "port": "{{server.port}}",
        "protocol": "{{server.protocol}}",
        "services": {
            "static-pages": false
        }
    },
    
    "plugins": [

    ],

    "tasks": [

    ]


}
