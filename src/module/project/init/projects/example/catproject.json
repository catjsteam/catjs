{
    "name": "{{name}}",

    "source": "src/",
    "target": "target/",
    "cattarget": "{{cattarget}}",

    "host": "{{host}}",
    "port": "{{port}}",
    "protocol": "{{protocol}}",

    "apppath": "{{appath}}",
    "appserver": {
        "host": "{{appserver.host}}",
        "port": "{{appserver.port}}",
        "protocol": "{{appserver.protocol}}"
    },

    "plugins": [

    ],

    "tasks": [

    ]


}
